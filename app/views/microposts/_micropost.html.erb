<li id="micropost-<%= micropost.id %>">
  <%= link_to gravatar_for(micropost.user, size: 50), micropost.user %>
  <span class="user"><%= link_to micropost.user.company_name, micropost.user %></span>
  <span class="content">プロジェクトの概要：<%= micropost.summary %></span>
  <span class="content">プロジェクトの目的：<%= micropost.purpose %></span>
  <span class="content">プロジェクトの対象：<%= micropost.target %></span>
  <span class="content">プロジェクトの期間(月/年)：<%= micropost.period %></span>
  <span class="content">プロジェクトの会議場所/開催場所：<%= micropost.place %></span>
  <span class="content">プロジェクトの報酬(￥)：<%= micropost.reward %></span>
  <span class="content">プロジェクトの応募資格：<%= micropost.qualification %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
    <% if current_user == micropost.user %>
      <%= link_to "delete", micropost, method: :delete,
                                       data: { confirm: "You sure?" } %>
    <% end %>
  </span>
</li>